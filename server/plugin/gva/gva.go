package gva

// Placeholder module.
//
// Some upstream gin-vue-admin forks reference a built-in "gva" plugin package via blank import.
// This workspace bootstrap does not ship that plugin, but we keep an empty package so builds/tests succeed.

